# IoT Platform Frontend

Next.js frontend for the rule-based predictive maintenance platform.

## Development

Install dependencies (requires internet access):

```bash
npm install
```

Run the development server:

```bash
npm run dev
```

Build for production:

```bash
npm run build
```

Run tests (none defined yet):

```bash
npm test
```
